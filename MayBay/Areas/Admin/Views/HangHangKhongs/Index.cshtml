@model List<MayBay.Models.HangHangKhong>

@{
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" integrity="sha512-pdKg0WXWNAdn4IVpvjPRzPqyp+22Vj4J6VFI4FxCk99i3LepJcE5r7Q1pAMarPr/RmuOsAxVQkD0Dh7+L4Zww==" crossorigin="anonymous" referrerpolicy="no-referrer" />

<style>
    body {
        font-family: 'Arial', sans-serif;
        background-color: #f0f8ff;
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .list-title {
        text-align: center;
        background-color: #20b2aa;
        color: white;
        padding: 20px;
        font-size: 24px;
        margin-bottom: 20px;
    }

    .action-links a {
        color: #20b2aa;
        margin-right: 10px;
        text-decoration: none;
    }

        .action-links a:hover {
            text-decoration: underline;
        }

    .card {
        width: 22rem;
        height: 100%;
        margin-bottom: 20px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        display: flex;
        flex-direction: column;
        background-color: #fffacd;
        border-radius: 8px;
    }

    .card-header {
        background-color: #20b2aa;
        color: white;
        font-weight: bold;
        flex: 0 0 auto;
        padding: 15px;
        border-top-left-radius: 8px;
        border-top-right-radius: 8px;
    }

    .card-body {
        padding: 15px;
        flex: 1 1 auto;
    }

    .card-footer {
        flex: 0 0 auto;
        padding: 15px;
        border-bottom-left-radius: 8px;
        border-bottom-right-radius: 8px;
    }
    .card-body .hotel-image {
        max-width: 100%;
        max-height: 100%;
        object-fit: contain;
    }
</style>

<div class="list-title">
    <h1>Danh Sách Hãng Hàng Không</h1>
</div> 

<p>
    @Html.ActionLink("Thêm Hãng Hàng Không mới", "Create", null, new { @class = "btn btn-primary" })
</p>

<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-4">
            <div class="card">
                <div class="card-header">
                    @Html.DisplayFor(modelItem => item.TenHH)
                </div>
                <div class="card-body">
                    <img src="/hinh/@item.HinhAnh" alt="@item.HinhAnh" class="hotel-image" />
                    <div class="hotel-details">
                        <label>Tên Hãng Hàng Không:</label> @Html.DisplayFor(modelItem => item.TenHH)
                    </div>
                    <div class="hotel-details">
                        <label>Mã Hãng Hàng Không:</label> @Html.DisplayFor(modelItem => item.MaHangHang)
                    </div>
                </div>
                <div class="card-footer">
                    <div class="action-links">
                        @Html.ActionLink("Chỉnh sửa", "Edit", new { id = item.MaHangHang }, new { @class = "btn btn-outline-primary" }) |
                        @Html.ActionLink("Xóa", "Delete", new { id = item.MaHangHang }, new { @class = "btn btn-outline-danger" }) |
                    </div>
                </div>
            </div>
        </div>
    }
</div>